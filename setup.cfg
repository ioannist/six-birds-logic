[metadata]
name = emergent-logic
version = 0.0.0

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.10
install_requires =
    numpy
    scipy
    pandas
    networkx
    matplotlib

[options.extras_require]
dev =
    pytest
    ruff

[options.packages.find]
where = src
